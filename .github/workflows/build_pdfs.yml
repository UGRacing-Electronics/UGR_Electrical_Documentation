name: Compile LaTeX Files

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  trigger-compile-workflow:
    uses: UGRacing-Electronics/UGR_Electrical_Documentation/.github/workflows/build_LaTeX.yml@main
    with:
      event-type: github.event_name
