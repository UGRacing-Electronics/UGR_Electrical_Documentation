name: Compile LaTeX Files

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  trigger-compile-workflow:
    uses: UGRacing-Electronics/UGR_Electrical_Management_Documentation/.github/workflows/build_LaTeX.yml@a224bb561d67de360a156dd7499696cf972a0fe9
    with:
      event-type: github.event_name
