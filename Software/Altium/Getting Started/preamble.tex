\documentclass[12pt,a4paper]{article}
% \usepackage[utf-8]{inputenc}
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{fancyhdr}
\usepackage{fontspec}
\usepackage{eso-pic}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{cleveref}

\setlength{\parindent}{0pt}

\setmainfont{proxima-nova-regular.ttf}[
Path = ./fonts/,
BoldFont = proxima-nova-semibold.ttf,
ItalicFont = proxima-nova-regular-italic.otf,
BoldItalicFont  = proxima-nova-semibold-italic.otf,
Scale = 1.0]

% Define colors
\definecolor{lightergray}{RGB}{242,242,242}
\definecolor{lightgray}{RGB}{217,217,217}
\definecolor{mediumgray}{RGB}{191,191,191}
\definecolor{brightyellow}{RGB}{255,204,0}

% Background shapes for every page
\AddToShipoutPictureBG{%
  \begin{tikzpicture}[remember picture,overlay]
    \ifnum\value{page}=1
    % Top shapes
    % Light gray triangle (left top)
        \fill[mediumgray] (current page.north west) -- 
                         ($(current page.north west) + (0,-6cm)$) -- 
                         ($(current page.north west) + (6cm,0)$) -- cycle;
        
        % White paper shape in middle (with rounded corners)
        \fill[lightgray] (current page.north west) -- 
                     ($(current page.north west) + (0,-3cm)$) -- 
                     ($(current page.north east) + (3cm,0)$) -- cycle;
        
        % Yellow triangle (right top)
        \fill[brightyellow] (current page.north east) -- 
                            ($(current page.north east) + (-5.5cm,0)$) -- 
                            ($(current page.north east) + (0,-5.5cm)$) -- cycle;
        
        % Bottom shapes
        % Medium gray triangle (left bottom)
        \fill[lightergray] (current page.south west) -- 
                          ($(current page.south west) + (0,6cm)$) -- 
                          ($(current page.south west) + (6cm,0)$) -- cycle;
        
        % White paper shape in middle (with rounded corners)
        \fill[lightgray] (current page.south west) -- 
                     ($(current page.south west) + (0,1.5cm)$) -- 
                     ($(current page.south east) + (3cm,3.5cm)$) -- 
                     ($(current page.south east) - (0cm,-0cm)$) -- cycle;
    
        % Light gray triangle (right bottom)
        \fill[mediumgray] (current page.south east) -- 
                         ($(current page.south east) + (-7cm,0)$) -- 
                         ($(current page.south east) + (0,7cm)$) -- cycle;
     \else
    % Top shapes
    % Light gray triangle (left top)
        \fill[mediumgray] (current page.north west) -- 
                         ($(current page.north west) + (0,-2.7cm)$) -- 
                         ($(current page.north west) + (2.7cm,0)$) -- cycle;
        
        % Light Gray (top middle)
        \fill[lightgray] (current page.north west) -- 
                     ($(current page.north west) + (0,-1.5cm)$) -- 
                     ($(current page.north east) + (1cm,0)$) -- cycle;
        
        % Yellow triangle (right top)
        \fill[brightyellow] (current page.north east) -- 
                            ($(current page.north east) + (-2.5cm,0)$) -- 
                            ($(current page.north east) + (0,-2.5cm)$) -- cycle;
        
        % Bottom shapes
        % Medium gray triangle (left bottom)
        \fill[lightergray] (current page.south west) -- 
                          ($(current page.south west) + (0,2cm)$) -- 
                          ($(current page.south west) + (2cm,0)$) -- cycle;
        
        % White paper shape in middle (with rounded corners)
        \fill[lightgray] (current page.south west) -- 
                     ($(current page.south west) + (0,0.2cm)$) -- 
                     ($(current page.south east) + (3cm,2cm)$) -- 
                     ($(current page.south east) - (0cm,-0cm)$) -- cycle;
    
        % Light gray triangle (right bottom)
        \fill[mediumgray] (current page.south east) -- 
                         ($(current page.south east) + (-4cm,0)$) -- 
                         ($(current page.south east) + (0,4cm)$) -- cycle;

        \node at ($(current page.south east) + (-1.2cm,1.2cm)$) {
            \includegraphics[width=1.3cm]{template/white_hex.png}
            % \textbf{\small LOGO}
        };
    \fi
  \end{tikzpicture}
}

% Page style
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}